# 🎤 Voice King - Голосовий Асистент для Windows

Локальний голосовий асистент з підтримкою Claude та ChatGPT для розпізнавання намірів користувача.

## 🚀 Можливості

### Core функції
- ✅ **Розпізнавання мовлення** (STT) через Whisper - локально, підтримка української
- ✅ **Синтез мовлення** (TTS) через pyttsx3 - озвучення результатів
- ✅ **Вибір AI провайдера**: Claude (Haiku) або ChatGPT (GPT-4o-mini)
- ✅ **Wake Word Detection** - активація кодовим словом через Porcupine

### Команди
- ✅ **Запуск програм** - `"відкрий Chrome"`, `"запусти калькулятор"`
- ✅ **Відкриття папок** - `"відкрий папку Downloads"`
- ✅ **Гарячі клавіші** - `"натисни Ctrl+S"`, `"Alt+F4"`, `"Win+D"`
- ✅ **Керування гучністю** - `"зроби гучніше"`, `"звук на 50%"`, `"вимкни звук"`
- ✅ **Перемикання вікон** - `"переключись на Chrome"`
- ✅ **Вставка тексту** - `"вставити текст: Hello World"`
- ✅ **Макроси** - послідовності команд (робочий режим, тощо)

### Безпека
- ✅ **Підтвердження** для небезпечних операцій (shutdown, restart)
- ✅ **Whitelist** додатків і папок - AI не може запускати невідомі програми

## 📋 Вимоги

- **Python 3.8+**
- **Windows** (основна підтримка, Linux/Mac частково)
- **API ключ**: Claude або OpenAI
- **Мікрофон** (для голосового режиму)

## 🔧 Встановлення

### 1. Клонувати репозиторій

```bash
git clone <repository_url>
cd voice_king
```

### 2. Створити віртуальне середовище

```bash
python -m venv venv
venv\Scripts\activate  # Windows
# source venv/bin/activate  # Linux/Mac
```

### 3. Встановити залежності

```bash
pip install -r requirements.txt
```

### 4. Налаштувати конфігурацію

Скопіювати приклад конфігу:

```bash
copy config\config.example.json config\config.json  # Windows
# cp config/config.example.json config/config.json  # Linux/Mac
```

Відредагувати `config/config.json`:

```json
{
  "ai_provider": "claude",  // або "openai"

  "claude": {
    "api_key": "sk-ant-...",  // Ваш Claude API ключ
    "model": "claude-3-haiku-20240307"
  },

  "openai": {
    "api_key": "sk-...",  // Ваш OpenAI API ключ
    "model": "gpt-4o-mini"
  },

  "apps": {
    "chrome": "C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe",
    "notepad": "notepad.exe"
    // Додайте свої програми
  },

  "folders": {
    "downloads": "C:\\Users\\YourUsername\\Downloads",
    "documents": "C:\\Users\\YourUsername\\Documents"
    // Додайте свої папки
  }
}
```

## 🎯 Використання

### 1. Текстовий режим (для тестування)

```bash
python main.py
```

Вводьте команди текстом:
```
Ви: відкрий хром
Ви: звук на 50 відсотків
Ви: натисни Alt+Tab
Ви: exit
```

### 2. Голосовий режим (з кнопкою)

```bash
python main.py --voice
```

Натискайте Enter → говоріть команду → чекайте розпізнавання.

### 3. Wake Word режим (hands-free!)

```bash
python main.py --wake-word YOUR_PICOVOICE_KEY
```

Скажіть "Porcupine" або "Computer" → дайте команду → асистент виконає.

**Отримати Picovoice ключ:**
1. Зареєструйтесь на https://console.picovoice.ai/
2. Створіть безкоштовний Access Key
3. Використовуйте в команді вище

## 📝 Приклади команд

### Запуск програм
```
"відкрий Chrome"
"запусти блокнот"
"відкрий калькулятор"
"запусти VS Code"
```

### Навігація
```
"відкрий папку Downloads"
"відкрий папку Documents"
"переключись на Chrome"  // активація вікна
```

### Керування гучністю
```
"зроби гучніше"           // +10%
"зменши звук на 20 відсотків"
"звук на 50 відсотків"    // встановити 50%
"вимкни звук"             // mute/unmute
```

### Гарячі клавіші
```
"натисни Enter"
"Ctrl+S"                  // зберегти
"Alt+Tab"                 // переключити вікно
"Win+D"                   // згорнути всі вікна
"Alt+F4"                  // закрити вікно
```

### Вставка тексту
```
"вставити текст: Привіт, як справи?"
"вставити текст: https://example.com"
```

### Макроси
```
"запусти робочий режим"  // VS Code + Chrome + звук 30%
```

### Системні команди
```
"вимкни комп'ютер"       // запитає підтвердження
"перезавантаж"           // запитає підтвердження
```

## 🏗️ Архітектура

```
┌─────────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  Мікрофон   │───▶│   STT    │───▶│   AI     │───▶│ Executor │
│             │    │ (Whisper)│    │(Claude/  │    │          │
└─────────────┘    └──────────┘    │ ChatGPT) │    └────┬─────┘
                                    └──────────┘         │
                                                         ▼
                   ┌──────────┐                   ┌──────────┐
                   │   TTS    │◀──────────────────│  Actions │
                   │(pyttsx3) │                   │  (OS)    │
                   └──────────┘                   └──────────┘
```

## 🔑 Отримання API ключів

### Claude (Anthropic)
1. Перейдіть на https://console.anthropic.com/
2. Створіть акаунт
3. Отримайте API ключ в розділі API Keys
4. **Вартість**: ~$0.25 за 1M токенів (Haiku)
5. **Безкоштовно**: $5 кредит на старт

### OpenAI (ChatGPT)
1. Перейдіть на https://platform.openai.com/
2. Створіть акаунт
3. Додайте платіжний метод
4. Отримайте API ключ
5. **Вартість**: ~$0.15 за 1M токенів (GPT-4o-mini)

### Picovoice (Wake Word)
1. Перейдіть на https://console.picovoice.ai/
2. Створіть безкоштовний акаунт
3. Отримайте Access Key
4. **Безкоштовно**: до 3 пристроїв
5. **Доступні слова**: porcupine, computer, jarvis, alexa, та інші

## 📁 Структура проекту

```
voice_king/
├── src/
│   ├── ai_providers/         # AI провайдери
│   │   ├── base.py          # Базовий клас
│   │   ├── claude_provider.py
│   │   └── openai_provider.py
│   ├── audio/               # STT та TTS
│   │   ├── stt.py          # Whisper STT
│   │   └── tts.py          # Text-to-Speech
│   ├── executor/            # Виконання команд
│   │   └── command_executor.py
│   └── config_loader.py     # Завантаження конфігу
├── config/
│   ├── config.example.json  # Приклад конфігу
│   └── system_prompt.txt    # Промпт для AI
├── main.py                  # Головний файл
├── requirements.txt         # Залежності
└── README.md               # Ця інструкція
```

## ⚙️ Налаштування

### Вибір моделі Whisper

У `config.json` змініть `audio.stt_model`:

- `tiny` - найшвидша, менш точна (~40MB)
- `base` - **рекомендовано** - баланс швидкості/точності (~75MB)
- `small` - точніша, повільніша (~240MB)
- `medium` - дуже точна, потребує більше часу (~770MB)

### Мова

У `config.json`:
```json
{
  "language": "uk-UA"  // українська
  // "language": "en-US"  // англійська
}
```

## 🛠️ Що працює / В розробці

### ✅ Реалізовано
- ✅ Wake word detection (Porcupine)
- ✅ Гарячі клавіші (pyautogui)
- ✅ Керування гучністю (pycaw для Windows, amixer для Linux)
- ✅ Перемикання між вікнами (pywinauto)
- ✅ Вставка тексту (pyautogui)
- ✅ Claude + OpenAI інтеграція
- ✅ Whisper STT (локально)
- ✅ TTS озвучення

### 🚧 Планується
- [ ] Пошук файлів (Everything API для Windows)
- [ ] Веб-пошук через браузер
- [ ] GUI інтерфейс
- [ ] Історія команд
- [ ] Плагін система
- [ ] Підтримка користувацьких wake words

## 🐛 Відомі проблеми

1. **Windows**: TTS може не знайти український голос - встановіть додаткові мовні пакети
2. **Linux**: Деякі функції (відкриття програм) потребують налаштування
3. **Латентність**: 1-3 сек від команди до виконання (нормально для cloud AI)

## 📊 Продуктивність

- **STT (Whisper base)**: ~1-2 сек для 5 сек аудіо
- **AI (Claude Haiku)**: ~200-500ms
- **AI (GPT-4o-mini)**: ~300-600ms
- **Загальна латентність**: 1-3 сек

## 💰 Вартість використання

При ~1000 команд/день:
- **Claude Haiku**: ~$0.75/місяць
- **GPT-4o-mini**: ~$0.45/місяць

## 🔒 Безпека

- API ключі зберігаються локально в `config/config.json` (не коммітяться в git)
- Небезпечні операції (shutdown) вимагають підтвердження
- Всі команди логуються для аудиту

## 🤝 Внесок

Pull requests вітаються! Для великих змін спочатку відкрийте issue.

## 📄 Ліцензія

MIT

## 👨‍💻 Автор

Voice King - голосовий асистент з AI мозком

---

**Потрібна допомога?** Створіть Issue на GitHub
